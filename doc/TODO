add tests
    for expression package
    for macro package

    for processors package
        comment processor
        newline processor
        operator processor
        whitespace processor

    for preprocessor

TODO
    in ExpressionPreprocessor
    in MacroExpansionService

investigate if whitespaces make sense for macro processing
add support for vararg macros
